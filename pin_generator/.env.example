# Pin Generator
# Called by multi-domain-clean. In multi-domain-clean/.env set:
#   PIN_API_URL=http://<this-host>:5000

# Run API: python generator.py --serve --port 5000
# Optional: AI keys, R2_* for upload (see generator.py)
